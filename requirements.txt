# 科学计算器依赖包

# 核心依赖
pyinstaller>=5.0  # 用于打包exe文件

# Windows 特定依赖
pywin32>=305; sys_platform == 'win32'      # Windows系统API支持

# 跨平台数学库
numpy>=1.24.0   # 强大的数学计算（可选，如需使用可取消注释）

# 可选：跨平台科学计算功能
# scipy>=1.10.0   # 科学计算功能（可选）
# matplotlib>=3.7.0  # 绘图功能（可选）
